# TRUVORNEX — UI + BACKEND FIX PROMPT
# DO NOT change any existing theme, colors, fonts, or styling outside what is explicitly listed below.

---

## TASK 1 — Load FK Screamer font

In `index.html` inside `<head>`, add:
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link href="https://fonts.googleapis.com/css2?family=Faster+One&display=swap" rel="stylesheet" />
```

Note: FK Screamer is a paid font. Use "Faster One" from Google Fonts as a free equivalent — same impact, same condensed display style.

In `src/index.css`, add:
```css
.font-screamer {
  font-family: 'Faster One', cursive;
}
```

---

## TASK 2 — Rewrite `src/pages/Login.jsx`

Complete rewrite. Rules:
- DO NOT change any color, background, border, or shadow values
- Left panel: animated sliding cards (auto-advance every 3.5 seconds, smooth fade+slide transition)
- Right panel: existing sign in / sign up form, unchanged except the white button fix below
- Remove ALL fake stat cards (2,400+ Verified Providers, AI-Powered Matching, Book in 60 Seconds, 4.9★) from the left panel entirely

Left panel sliding content — 4 slides, each with a headline + one line of supporting text:

Slide 1:
- Headline: "Your neighborhood, connected." — use className `font-screamer text-5xl`
- Body: "Services, trust, and community — all in one place."

Slide 2:
- Headline: "Every neighbor has a skill."
- Body: "Book verified providers from your own zone."

Slide 3:
- Headline: "Simon knows your area."
- Body: "AI-powered dispatch that understands local demand."

Slide 4:
- Headline: "Built for Hyderabad. Built for Helsinki."
- Body: "Two cities. One trust infrastructure."

Slide animation: each slide fades in from bottom (translateY 20px → 0, opacity 0 → 1), holds for 3.5 seconds, fades out upward. Use CSS transitions only, no new animation libraries.

Bottom of left panel: small dot indicators showing current slide (existing dot style if present, otherwise 4 small white circles, active one fully opaque, others at 30% opacity).

Fix the Sign In button: change from white background to `bg-transparent border border-white/30 text-white hover:bg-white/10` — must match the dark monochrome system.

---

## TASK 3 — Remove onboarding slides, allow guest browsing

In `src/pages/Onboarding.jsx`: delete the entire multi-step slide flow. Replace the whole file with a single redirect:
```jsx
import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
export default function Onboarding() {
  const navigate = useNavigate();
  useEffect(() => { navigate('/', { replace: true }); }, []);
  return null;
}
```

In `src/lib/AuthContext.jsx`: remove any logic that redirects new users to `/onboarding` after signup. After signup, navigate to `/` (home) directly.

In `src/App.jsx`: wrap all protected routes with a guest-friendly check — if a route currently redirects unauthenticated users to `/login`, change it to allow browsing but show a "Sign in to continue" prompt only when the user tries to take an action (book, buy, chat, etc.). The home page, services listing, neighborhood listing, marketplace browsing, provider profiles, and trust passport must all be accessible without login.

---

## TASK 4 — Fix Chat (messages not working after account creation)

Problem: chat_messages table uses `sender_id` and `receiver_id` (UUIDs) but the frontend may be sending email strings, or the thread_key is not being constructed consistently.

In `server/chat.js`:

Fix `GET /api/chat/conversations`:
```js
router.get('/conversations', async (req, res) => {
    const userId = req.session.user.id;
    try {
        const { rows } = await pool.query(`
            SELECT DISTINCT ON (thread_key)
                cm.thread_key,
                cm.content AS last_message,
                cm.created_at AS last_message_at,
                cm.read,
                CASE WHEN cm.sender_id = $1 THEN cm.receiver_id ELSE cm.sender_id END AS other_user_id,
                u.full_name AS other_user_name,
                u.avatar_url AS other_user_avatar
            FROM chat_messages cm
            JOIN users u ON u.id = CASE WHEN cm.sender_id = $1 THEN cm.receiver_id ELSE cm.sender_id END
            WHERE cm.sender_id = $1 OR cm.receiver_id = $1
            ORDER BY thread_key, cm.created_at DESC
        `, [userId]);
        res.json({ conversations: rows });
    } catch (err) { res.status(500).json({ error: err.message }); }
});
```

Fix `POST /api/chat/messages` — ensure thread_key is always constructed as sorted UUIDs:
```js
router.post('/messages', async (req, res) => {
    const senderId = req.session.user.id;
    const { receiver_id, content, type } = req.body;
    if (!receiver_id || !content) return res.status(400).json({ error: 'receiver_id and content required' });
    const threadKey = [senderId, receiver_id].sort().join('_');
    try {
        const { rows } = await pool.query(
            `INSERT INTO chat_messages (sender_id, receiver_id, thread_key, content, type)
             VALUES ($1, $2, $3, $4, $5) RETURNING *`,
            [senderId, receiver_id, threadKey, content, type || 'text']
        );
        res.json({ message: rows[0] });
    } catch (err) { res.status(500).json({ error: err.message }); }
});
```

Fix `GET /api/chat/messages/:threadKey` — verify user is part of the thread:
```js
router.get('/messages/:threadKey', async (req, res) => {
    const userId = req.session.user.id;
    const { threadKey } = req.params;
    if (!threadKey.includes(userId)) return res.status(403).json({ error: 'Not your conversation' });
    try {
        const { rows } = await pool.query(
            `SELECT cm.*, u.full_name AS sender_name, u.avatar_url AS sender_avatar
             FROM chat_messages cm JOIN users u ON u.id = cm.sender_id
             WHERE cm.thread_key = $1 ORDER BY cm.created_at ASC`,
            [threadKey]
        );
        await pool.query(`UPDATE chat_messages SET read = true WHERE thread_key = $1 AND receiver_id = $2`, [threadKey, userId]);
        res.json({ messages: rows });
    } catch (err) { res.status(500).json({ error: err.message }); }
});
```

---

## TASK 5 — Fix Profile section (empty after account creation)

Problem: after signup, `profiles` table or `users` extended fields are not populated. The profile page queries fields that don't exist for new users.

In `server/index.js`, in the signup route, after inserting the user, also:
1. Create their wallet automatically:
```js
await pool.query(
    `INSERT INTO wallets (user_id, balance, currency) VALUES ($1, 0, 'PKR') ON CONFLICT DO NOTHING`,
    [rows[0].id]
);
```
2. Create their trust score row if provider:
```js
if (role === 'provider') {
    await pool.query(
        `INSERT INTO provider_trust_scores (provider_id, score, tier) VALUES ($1, 0, 'new') ON CONFLICT DO NOTHING`,
        [rows[0].id]
    );
}
```

In `src/pages/customer/CustomerProfile.jsx` (or equivalent profile page): make sure every field has a fallback for null/empty values — never crash on missing data. Add an "Edit Profile" form that PUTs to a new route:

Add to `server/index.js`:
```js
app.put('/api/auth/profile', requireAuth, async (req, res) => {
    const { full_name, phone, city, avatar_url } = req.body;
    const { rows } = await pool.query(
        `UPDATE users SET full_name=$1, phone=$2, city=$3, avatar_url=$4, updated_at=NOW()
         WHERE id=$5 RETURNING id, email, full_name, phone, city, avatar_url, role`,
        [full_name, phone, city, avatar_url, req.session.user.id]
    );
    req.session.user = { ...req.session.user, ...rows[0] };
    res.json({ user: rows[0] });
});
```

---

## TASK 6 — Fix Marketplace (cannot add product)

Problem: the create listing form in `src/pages/Marketplace.jsx` is likely not sending the correct fields or not passing auth.

In `src/pages/Marketplace.jsx`, find the create listing form submit handler and fix the fetch call:
```js
const response = await fetch('/api/marketplace', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    credentials: 'include', // THIS IS CRITICAL — session cookie must be sent
    body: JSON.stringify({
        title,
        description,
        price_pkr: parseFloat(price),
        category,
        condition,
        negotiable,
        zone_id: null
    })
});
```

Every fetch call that hits a protected route must include `credentials: 'include'` — audit all fetch calls in `Marketplace.jsx`, `Chat.jsx`, `Wallet.jsx`, and `CustomerProfile.jsx` and add `credentials: 'include'` to every one that doesn't have it.

---

## TASK 7 — Fix Wallet (cannot access after account creation)

In `server/wallet.js`, `GET /` route — wallet may not exist yet for the user. Add auto-creation:
```js
router.get('/', async (req, res) => {
    const userId = req.session.user.id;
    try {
        // Auto-create wallet if missing
        await pool.query(
            `INSERT INTO wallets (user_id, balance, currency) VALUES ($1, 0, 'PKR') ON CONFLICT (user_id, currency) DO NOTHING`,
            [userId]
        );
        const { rows } = await pool.query(
            `SELECT w.*, COALESCE(SUM(ll.coins), 0) AS loyalty_coins
             FROM wallets w
             LEFT JOIN loyalty_ledger ll ON ll.user_id = w.user_id
             WHERE w.user_id = $1 AND w.currency = 'PKR'
             GROUP BY w.id`,
            [userId]
        );
        res.json({ wallet: rows[0] });
    } catch (err) { res.status(500).json({ error: err.message }); }
});
```

Also add `credentials: 'include'` to every wallet-related fetch in the frontend.

---

## TASK 8 — Remove "Made in Bolt" badge

Search entire codebase for the Bolt badge and remove it:
```bash
grep -ril "bolt\|Made in Bolt\|bolt-badge" src/ public/ index.html --include="*.jsx" --include="*.js" --include="*.html" --include="*.css"
```
Delete every reference. If it's injected via a script tag in `index.html`, remove that script tag entirely.

---

## WHAT NOT TO DO
- Do not change any color values, background colors, border colors, or shadows anywhere
- Do not change any font sizes except the hero headline in Login.jsx (font-screamer class)
- Do not install new npm packages — use only what is already in package.json
- Do not change any existing working routes in server/index.js
- Do not touch the design system in index.css except adding the .font-screamer class