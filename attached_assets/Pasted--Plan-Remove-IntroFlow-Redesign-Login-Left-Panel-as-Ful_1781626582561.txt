**Plan: Remove IntroFlow, Redesign Login Left Panel as Full-Slide Carousel**

**1. Remove IntroFlow from App.jsx**

- Delete the IntroFlow import at the top of App.jsx 
- Remove the introDone state and handleIntroComplete function entirely
- Remove the line {splashDone && !introDone && <IntroFlow onComplete={handleIntroComplete} />} from the render
- The SplashScreen is separate and stays untouched

**2. Redesign Login.jsx Left Panel**

Replace the current left panel (which just has one small sliding text line) with a rich full-slide experience copied from IntroFlow — the exact 4 slides with icon, badge, headline, subtitle, and 3 feature rows:

- Move the full 4-slide data (badge, title, subtitle, Visual icon, features array) from IntroFlow.jsx into Login.jsx 
- Add the missing icon imports (Building2, Home, Handshake, Rocket, Shield, Clock, MapPin, Star, Users, Globe, Sparkles, CheckCircle2, Briefcase)
- Left panel uses the same dark atmospheric background that IntroFlow uses (#080808 gradient with grid + radial glows)
- Each slide renders: large icon box (with spinning outer ring), badge pill, big headline, subtitle text, and 3 feature rows — all styled exactly like IntroFlow
- **Font**: Use Plus Jakarta Sans at font-weight: 800, font-size: clamp(1.65rem, 5vw, 2.2rem) — no Faster One at all
- **Transitions**: Slide content transitions with translateX (slide in from right, exit to left) — same as IntroFlow's exitDir logic, not just fade
- Dot indicators at the bottom of the left panel (same pill dots as IntroFlow)
- Auto-advance every 3.5 seconds
- Remove all references to font-screamer class from Login.jsx

**3. Fix the Sign In button to be visible against the right panel**

- The right panel has var(--color-bg) background (dark in dark mode, white in light mode)
- Change the submit button back to use var(--color-primary) background + var(--color-on-primary) text — the white-border transparent style we added is only visible on the dark left panel, not on the right

**Summary**: The IntroFlow screen disappears permanently. Its 4 slides move inside the Login page left panel, making it a full-featured onboarding carousel that users see when they visit /login. The right side stays as the sign-in/sign-up form. Users who don't want to sign in can navigate away freely (guest browsing is already enabled). The bad "Faster One" font is replaced with the existing premium Plus Jakarta Sans at heavy weight, matching the IntroFlow's clean look exactly.

Switch back to **Build** mode using the "Implement this plan" button to apply these changes.